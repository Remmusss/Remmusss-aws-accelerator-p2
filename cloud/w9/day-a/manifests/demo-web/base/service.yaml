apiVersion: v1
kind: Service
metadata:
  name: demo-web
  namespace: lab
  labels:
    app: demo-web
    app.kubernetes.io/name: demo-web
    app.kubernetes.io/part-of: w9-platform
spec:
  type: NodePort
  selector:
    app: demo-web
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 32123
