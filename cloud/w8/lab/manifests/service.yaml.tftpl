apiVersion: v1
kind: Service
metadata:
  name: demo-web
  namespace: lab
spec:
  type: NodePort
  selector:
    app: demo-web
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: ${nodeport}
